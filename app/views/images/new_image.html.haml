.item_image_new
  .item_image_new__upload
    = form_with model: @image, id:"item_new", local: true do |f|
      = f.file_field :image,class:"add_image"
      = f.hidden_field :item_id , value:@item.id 
      .remove_btn
        =  icon "fa","trash",class:"delete_image"
      = f.submit value:"登録する" ,class:"image_register"
  .item_image_new__preview
    %h3.text 商品画像プレビュー
    .preview
